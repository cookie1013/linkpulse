server:
  port: 8080

spring:
  application:
    name: linkpulse

  datasource:
    url: jdbc:mysql://localhost:3306/linkpulse?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: linkpulse_user
    password: xuqi123456789

  jpa:
    hibernate:
    ddl-auto: update
    show-sql: true